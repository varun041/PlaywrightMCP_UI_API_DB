# Test data for features/system_health_and_error_handling.feature
# No hardcoded business data values are present in this feature file today
# (scenarios describe conditions narratively, e.g. "an unexpected
# server-side failure"). This file is kept for structural consistency with
# the other testdata/*.yml files and as the designated home for concrete
# values (e.g. specific malformed payloads) once these scenarios are
# implemented.